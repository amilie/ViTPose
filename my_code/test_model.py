"""
test_model.py — superseded by test_models.py which covers both architectures.

Run:
    python test_models.py
    python -m pytest test_models.py -v
"""

from test_models import *  # re-export so existing imports keep working
